/**
 * Spring {@code @Configuration} classes and typed configuration properties.
 *
 * <p>Currently: {@link com.pulsewallet.pulsewallet.config.CorsConfig}.
 *
 * <p>Planned: {@code SecurityConfig} and {@code JwtProperties} (Milestone 2),
 * {@code RestClientConfig} for the fraud-detection service (Milestone 5),
 * {@code WebSocketConfig} for fraud alerts (Milestone 5), and
 * {@code OpenApiConfig} if Swagger UI is added.
 */
package com.pulsewallet.pulsewallet.config;
