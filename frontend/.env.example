# PulseWallet frontend environment
# Copy to .env and edit as needed. Never commit .env.
#
# The Vite dev server reads VITE_* variables at build time via import.meta.env.

# Spring Boot backend URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080
